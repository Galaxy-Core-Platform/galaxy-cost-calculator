# Boilerplate Templates Configuration
# Note: VITE_ prefix is required for Vite to expose env vars to the React app
# This is NOT part of the boilerplate, just how we tell the app where to find templates

# Base directory where all boilerplate templates are stored
VITE_TEMPLATES_BASE_PATH=/Users/mifo/Desktop/Galaxy

# Rust + Actix-web + PostgreSQL boilerplate location (relative to base path)
VITE_RUST_ACTIX_TEMPLATE_PATH=spark

# Backend API configuration
VITE_BACKEND_URL=http://localhost:3001